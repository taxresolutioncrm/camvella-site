# Frontend-safe values only
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Never place provider secrets or service-role keys in browser-exposed variables.
# Server/Edge Function secrets are configured only after the dedicated project/provider setup.
